# ----- PostgreSQL (consumido pelo docker-compose.yml) -----
POSTGRES_USER=ciesp
POSTGRES_PASSWORD=ciesp_dev_2026
POSTGRES_DB=ata_registro_precos
POSTGRES_PORT=5432

# ----- Prisma -----
# Host "localhost" porque a API roda fora do container e acessa a porta publicada.
DATABASE_URL="postgresql://ciesp:ciesp_dev_2026@localhost:5432/ata_registro_precos?schema=public"

# ----- API -----
PORT=3000
